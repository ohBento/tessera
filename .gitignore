node_modules
/dist
/release
.env
.env.*
!.env.example

# vitest browser mode writes failure screenshots here
__screenshots__
.vitest-attachments

# the design hook rewrites this cache on every edit; the rule config beside it is the part worth keeping
.impeccable/hook.cache.json
